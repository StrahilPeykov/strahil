---
title: A first note
date: 2026-06-20
summary: Placeholder article so the writing section renders.
draft: true
---

This is a placeholder article. It exists so the `/writing` section has something
to show and so the prose styling (this readable body font, sitting next to the
monospace furniture) is visible.

## A heading

A paragraph of body text. Long-form reading happens here, which is exactly why
the body uses a proportional font instead of monospace.

- list item one
- list item two

Inline `code` and a block:

```ts
const ok = true;
```

Replace this with something real whenever you want.
